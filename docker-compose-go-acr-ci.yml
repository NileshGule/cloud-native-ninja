version: "3"

services:
  techtalks.producer:
    image: ngacrregistry.azurecr.io/techtalksproducer:${github.sha}
    build:
      context: src/go/producer
      dockerfile: Dockerfile

  techtalks.consumer:
    image: ngacrregistry.azurecr.io/techtalksconsumer:${github.sha}
    build:
      context: src/go/consumer
      dockerfile: Dockerfile
